<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="com.example.faizhashmi.onlineconsultancy">

<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:windowSoftInputMode="stateHidden"
    android:theme="@style/AppTheme"
    android:name=".MyApp">
    <activity android:name=".SplashActivity" android:theme="@style/SplashTheme">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />

        </intent-filter>

    </activity>
    <activity android:name=".MainActivity" />
    <activity android:name=".RegionActivity" />
    <activity android:name=".OptionActivity" />
    <activity android:name=".ScholarshipActivity" />
    <activity android:name=".CountryActivity" />
    <activity android:name=".UniversityActivity" />
    <activity android:name=".UniActivity" />
    <activity android:name=".UniiActivity" />
    <activity android:name=".ScholarActivity" />






</application>

</manifest>

